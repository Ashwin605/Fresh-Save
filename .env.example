# ============================================
# FreshSave — Environment Configuration
# ============================================
# Copy this file to .env and fill in your values.

# ---------- Application ----------
NODE_ENV=development
APP_PORT=3000
APP_NAME=FreshSave
API_PREFIX=api/v1
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# ---------- HTTPS & TLS Configuration ----------
# Set to 'true' to enable native HTTPS in NestJS.
HTTPS_ENABLED=false
# Provide absolute or relative paths to your certificates if HTTPS_ENABLED=true
TLS_KEY_PATH=/path/to/private-key.pem
TLS_CERT_PATH=/path/to/certificate.pem

# ---------- PostgreSQL (PostGIS) ----------
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=freshsave
DATABASE_PASSWORD=***REMOVED***
DATABASE_NAME=freshsave_db
DATABASE_URL=postgresql://${DATABASE_USER}:${DATABASE_PASSWORD}@${DATABASE_HOST}:${DATABASE_PORT}/${DATABASE_NAME}?schema=public

# ---------- Redis ----------
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# ---------- Logging ----------
LOG_LEVEL=debug

# ---------- Authentication ----------
JWT_ACCESS_SECRET=your_jwt_access_secret_key
JWT_REFRESH_SECRET=your_jwt_refresh_secret_key
JWT_ACCESS_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d
